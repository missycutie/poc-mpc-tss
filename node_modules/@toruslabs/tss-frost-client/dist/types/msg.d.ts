import type { MessageBroker as WasmMessageBroker } from "@toruslabs/tss-frost-lib";
import { Socket } from "socket.io-client";
export declare class MessageBroker implements WasmMessageBroker {
    session_id: string;
    sender_id: number;
    private messages;
    private sockets;
    constructor(session: string, sender: number);
    getSocket(server_id: number): Socket | undefined;
    connect(ids: number[], endpoints: string[]): Promise<void>;
    close(): void;
    send(to: number, mtype: string, mdata: Uint8Array): Promise<void>;
    receive(from: number, mtype: string): Promise<Uint8Array>;
}
